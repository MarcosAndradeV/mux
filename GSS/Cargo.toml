[package]
name = "gss"
version = "0.1.0"
edition = "2024"

[dependencies]
lex-just-parse = { version = "1.0.0" }

[features]
interning = ["lex-just-parse/interning"]
internal-api = []

[[bin]]
name = "gss_test"
path = "src/bin/main.rs"
