[package]
name = "muxui"
version = "0.1.0"
edition = "2024"

[dependencies]
gss = { path = "../GSS", version = "0.1.0", features = ["interning"] }
notify = "8.2.0"
# lex-just-parse = { version = "1.3.1", features = ["interning"] }
raylib = { path = "../raylib-rs", version = "0.1.0" }
